#include "Effect.h"

/*Effect* Effect::CompileFromSourceCode(const char* filename)
{
    std::ifstream stream;
    stream.open(filename, std::ifstream::in);
    if(!stream)
        throw GeexRendererException("Effect source file couldn't be opened");

    std::string fileContents;
    std::string line;

    while(std::getline(stream, line))
        fileContents += line;

    stream.close();

    Effect* effect = new 

    return effect;
}*/